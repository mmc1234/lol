package com.github.mmc1234.lol.render;

public enum TextureWarpMode {
    REPEAT,
    /**
     * clamp to border
     * */
    CLAMP,
    MIRROR,
    /**
     * clamp to edge
     * */
    MIRROR_ONCE;
}
