package com.github.mmc1234.lol.p1.render;

public enum TextureWarpMode {
    REPEAT,
    /**
     * clamp to border
     * */
    CLAMP,
    MIRROR,
    /**
     * clamp to edge
     * */
    MIRROR_ONCE;
}
